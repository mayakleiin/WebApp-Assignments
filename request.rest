@baseUrl=http://localhost:3000

### 1) Register
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "username": "maya",
  "email": "maya@test.com",
  "password": "123456"
}

### 2) Login  (copy accessToken + refreshToken from response)
POST {{baseUrl}}/auth/login
Content-Type: application/json

{
  "email": "maya@test.com",
  "password": "123456"
}

### ===== Paste tokens here after login =====
@accessToken = PASTE_ACCESS_TOKEN_HERE
@refreshToken = PASTE_REFRESH_TOKEN_HERE

### 3) Create Post (protected)
POST {{baseUrl}}/post
Content-Type: application/json
Authorization: Bearer {{accessToken}}

{
  "title": "Bad Romance",
  "content": "Caught in a bad romance"
}

### 4) Get All Posts
GET {{baseUrl}}/post

### 5) Get Post by ID (paste id)
@postId = PASTE_POST_ID_HERE
GET {{baseUrl}}/post/{{postId}}

### 6) Update Post (protected)
PUT {{baseUrl}}/post/{{postId}}
Content-Type: application/json
Authorization: Bearer {{accessToken}}

{
  "title": "Diamonds",
  "content": "Shine bright"
}

### 7) Delete Post (protected)
DELETE {{baseUrl}}/post/{{postId}}
Authorization: Bearer {{accessToken}}

### 8) Refresh tokens (use refreshToken)
POST {{baseUrl}}/auth/refresh
Content-Type: application/json

{
  "refreshToken": "{{refreshToken}}"
}

### 9) Logout (invalidate refresh token)
POST {{baseUrl}}/auth/logout
Content-Type: application/json

{
  "refreshToken": "{{refreshToken}}"
}
